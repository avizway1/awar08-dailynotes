

EC2 : Elastic Compute Cloud : 

We can launch instances without worrything about infrastructure.
Instance = Server = Azure VM = Google Engine = Virtual Machiene = box

1. On-Demand Instances : 
--> Pay-as-you-go - NO Commitment.
--> Dev/Test, Unpredictable workloads, New application testings.
--> Most expensive per hour.
--> AWS bills per second with a 60 sec minimum billing.

2. Reserved Instances (RI)
--> commit for 1/3 yrs, you get big discount
--> best for "steady", Predictable workloads.. always on kind of apps..

--> Standard RI : Locked to a specific instance type/region. Good Discount
--> Convertable RI : Can change instance family/size. Slightly loweer discount, more flexible.

Pricing Options for RI: 
	Full Upfront : pay 100% amount and get your instance.
	Partial upfront : pay 30-60% amount now, AWS reduce the hourly price.
	No Upfront : Pay everything based on usage.


3. Savings Plan : Savings Plans are a flexible pricing model that offer low prices on AWS usage, in exchange for a commitment to a consistent amount of usage (measured in $/hour) for a one- or three-year term. 

Compute Savings Plans : Applies to EC2 instance usage, AWS Fargate and AWS Lambda service usage, regardless of region, instance family, size, tenancy and operating system.

EC2 instance Savings Plans : Applies to instance usage within the committed EC2 family and region, regardless of size, tenancy and operating system.

Database Savings Plans : Applies to Aurora, RDS, DynamoDB, ElastiCache, Amazon DocumentDB, Neptune, Amazon OpenSearch Service, and other services regardless of supported engine, instance family, size, deployment option, or Region.

SageMaker Savings Plans: Applies to SageMaker service usage, regardless of Region, instance family and component.


4. Spot Instances : 
--> Bid for spare capacity, We can get huge discount
--> BUT These instances can be interrupted with just 2 min warning.
--> If our application interrupted, no affect, we can go with spot instances.

--> If price changed, AWS terminated the instance, we no need to pay for partial hours.
--> If price not changed, YOU terminated the instance, we no need to pay for Full duration.


=====


D: 07/04/2026


t3.micro

t --> family
3 --> Generation (Higher = Works better)
micro --> Size (micro, medium , large, xlarge, 12xlarge) (CPU & Memory)

i --> intel processor
a --> amd processor
g --> graviton processor


Instances Family :

General purpose : Balanced CPU + Memory + Network : T / M 
Compute Optimised : More CPU powered instances : C
Memory Optimised : Lots of RAM - Data lives in Memory : R, U, X, Z
Storage optimized : High IOPS / High throughput local storage : I, D, H
Accelerated Computing : GPU supported hardware : P, G, Trn
High-performance computing : High performance computing : hpc


Dev / QA : We use Slightly lower configurations
t3.medium
t4g.medium
t4g.large

UAT / Prod : We always matches Prod config in UAT env.
m5.large
m5.xlarge
m6i.large
r5.xlarge (rds - mysql)
r5.2xlarge (rds - postgresql)

=========

Step 1 : Add Tags  (Name, Project, ClientID, POC, Cost center)

Step 2 : Choose AMI (Amazon Machiene Image) : Microsoft Windows Server 2025 Base

Step 3 : Choose the Instance Type : c7i-flex.large / t3.small 

Step 5 : 

Keypair : Combination of Public and private key
Public Key = Lock					--> Stores inside ec2 Instance (AWS)
Private key = key to open lock		--> Downloads in our laptop (YOU)

windows - For password retrival, we use the private key
Linux - We can directly use the private key to connect

One keypair, can be used with multiple instances.

Step 6 : Network Configuration

--> use default vpc for now

Security group : Firewall for our instance.. 
Ports : 0 - 65535

Windows : RDP : Remote Desktop protocol : 3389
Linux : SSH : Secure Shell : 22
web : HTTP : 80
secure web : HTTPS : 443
mysql : 3306
mssql : 1433
postgre : 5432

From where you want to connect: 
1. Anywhere (0.0.0.0/0 :::/0) 
2. Custom (We can provide required network IPs)
3. MyIP


Step 7 : Choose the Storage 

EBS : Elastic Block Storage : 
Windows : 30 GB
Linux : 8 GB

Step 8 : Review and Launch


Windows OS : you can use "remote desktop tool"

Method 1 : Search --> "remote desktop" --> open application and provide the Instance Public IP, then provide the "Username" and "Password".. Click "Yes"..
Method 2 : Open run (Win+r) --> type "mstsc" --> Click enter.

MAC OS : Navigate to "app store" and Download "Windows App" then open.. provide the Instance Public IP, then provide the "Username" and "Password".. Click "Yes"..


Usermanagement : open run --> type "lusrmgr"



===========

Linux Instance launch:

1. Add tags
2. Choose "Amazon Linux AMI" (default username: ec2-user (redhat: redhat / ec2-user) (ubuntu: ubuntu / ec2-user))
3. Choose Instance type : t3.micro
4. Choose Keypair : existing / New keypair (.pem format)
5. Choose SG (Open SSH : 22)
6. Storage (8 gb )
7. Review and launch instance


Connectivity: 

Option 1 : Browser based connect : Choose instance, Click on "connect" then choose Connectt using public ip.
* Make sure your SG opens SSH anywhere.
* Check your browser pop-up
* WE DONT USE THIS OPTION IN REAL ENVIRONMENTS.

---------

Option 2 : 
Windows: Command Prompt / Powershell : (open cmd and type "ssh")
MacOS / Linux : Terminal

** MOSTLY WE USE THIS OPTION AT ENTERPRISE LEVEL.

If your cmd prompt says, ssh is not a recognised command.. then perform below steps:
Open Settings: Go to Settings > System > Optional features.
View Features: Click on View features next to "Add an optional feature".
Install OpenSSH: Search for "OpenSSH CLient," select the checkbox, click Next, and then Install.
CLose and open your command prompt.

Navigate to the location, where we have the keypair, Then open the Terminal / command prompt.. 

ssh -i yourkeypair.pem <username>@<public-ip>

ssh -i "awar08-kp.pem" ec2-user@3.110.169.105

Linux/mac : Permissions too open : chmod 400 keypair.pem
windows : 
icacls "C:\path\to\your\key" /inheritance:r
icacls "C:\path\to\your\key" /grant:r "$($env:USERNAME):(F)"
icacls "C:\path\to\your\key"

------

Option 3 : Works in Windows and MacOS

Install github in your laptop. THen use git bash to connect to your instance.

https://git-scm.com/install/windows
https://git-scm.com/install/mac

Navigate to the keypair location, then right click, more option, Open "git bash here"

ssh -i "awar08-kp.pem" ec2-user@3.110.169.105

----

Option 4 : Works only in Windows

Download Putty : https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html

Provide Public IP, Keep port 22

Connections --> SSH --> Auth --> Credentials --> browse the ppk file and connect

*Putty dont support .pem format, COnvert your .pem key to .ppk using "puttygen" tool.

---

Option 5 : MobaXterm 
* Less used at enterprise level.


=====================


/dev/nvme1n1

sudo file -s /dev/nvme1n1

sudo mkfs -t xfs /dev/nvme1n1

sudo mkdir ins1addvol

sudo mount /dev/nvme1n1 ins1addvol

**
If you copy an existing volume and try to attach the volume to the same ec2 instance.. You get a UUID error..

sudo xfs_repair -L /dev/nvme2n1

sudo xfs_admin -U generate /dev/nvme2n1

---

Snapshot : back up copy of an ebs volume.

Snapsots are point in time copies. (if you create a ss at 07:24, you get whatever data we have at 07:24)

AWS uses s3 as backend platform for SS.

Can we see data inside a SS .? Ans: NO.. (Create a volume and attach to instance..)
AWS uses incremental backup mechanism.. We just need most recent copy for all data.. 

If we have any unencrypted volume, to encrypt it.. Create a SS, then create a volume from the SS.. While creating, enable encryption.

----

RTO : Recovery Time Objective : 
Simple Meaning: Downtime limit
The Question : How long can we afford to be offline
Focus: Recovery speed
Example: If our RTO is 2 hours, We must have systems should be back - "up and running" with in 2 hours of crash to avaoid business damage.

RPO : Recovery Point Objective : 
Simple meaning: your data loss limit
Question: How much data can we afford to lose.
Focus: Backup frequency
Example : If your ROP is set to 1 hour, you must have backups configured for every hour.


Data Lifecycle Manager: Automate the creation, retention, copy and deletion of snapshots. 


======================

Golden AMI / Customised AMI : 

Req : need 10 ec2 instances with custom user, he needs pwd and sudo access.. Instance should have packages i.e; telnet, tree, httpd package, AV, internal sw (monitoring agent).. Addl volume should be mounted.. (* CIS benchmarks)

Option 1 : Launch 10 instances, connect to every instance and install all req s/w and perform customisations 10 times..  (for future req, do it again)

Option 2 : Launch 1 instance, connect to that instance and install all req s/w and perform customisations.. Then create a GoldenAMI.. Launch 10 instance.. (you can use it later)


1. user creation, passwd, ssh edited, sudo for f-id	--> working
2. httpd install, enable, and create the content 	--> working
3. installed tree, telnet							--> working
4. added vol, mounted and added entry in /fstab		--> working

---

Basic sanity check / Basic Validation

---

sudo file -s /dev/nvme1n1
sudo mkfs -t xfs /dev/nvme1n1
cd /mnt
sudo mkdir newvol
sudo mount /dev/nvme1n1 newvol

cat /etc/mtab | grep nvme1n1

/dev/nvme1n1 /mnt/newvol xfs rw,seclabel,relatime,attr2,inode64,logbufs=8,logbsize=32k,sunit=8,swidth=8,noquota 0 0

sudo vim /etc/fstab

/dev/nvme1n1 /mnt/newvol xfs rw,seclabel,relatime,attr2,inode64,logbufs=8,logbsize=32k,sunit=8,swidth=8,noquota 0 0


===========

recommended : stop the instance and create goldenAMI. Its not mandatory. 

=============

IMDSv2 : Instance Metadata Service

metadata : We can use this option to get data about the ec2 instance (When wea re connected)

To access this feature, first "Metadata accessible : Enabled"

2 Options : 

v1 : We can access information using metadata url (less secured - possibility for SSRF (server side request forgery) attack)
v2 : It needs token to access metadata.

v1: 
curl http://169.254.169.254/latest/meta-data/network/interfaces/macs/02:e8:ec:5a:f8:27/owner-id


ami-id
ami-launch-index
ami-manifest-path
block-device-mapping/
events/
hostname
identity-credentials/
instance-action
instance-id
instance-life-cycle
instance-type
local-hostname
local-ipv4
mac
managed-ssh-keys/
metrics/
network/
placement/
profile
public-hostname
public-ipv4
public-keys/
reservation-id
security-groups
services/
system
tag-sets/


v2 : We need to generate the token and need to use this while accessing metadata.

test using this url: 
curl -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600"

TOKEN=$(curl -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")

curl -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/ami-id

=================


Userdata: We can pass commands when we are launching instance for the first time.. 
usedata script run as root user..
--> verify logs at /var/log/cloud-init-output.log
--> size should not exceed 16KB


#!/bin/bash
sudo dnf install httpd -y
sudo systemctl start httpd
sudo systemctl enable httpd
echo "<h1>This website is from userdata </h1>" >> /var/www/html/index.html

========

EFS : Elastic File system : 
--> Also called as file share (general)
--> No Pre-Provisioning required. Unlimited storage.
--> We can use EFS only with LINUX OS.
--> For windows, we have a service names "FSx", it works with SMB protocol (Server Message Block).. 


--> Make sure to use same security group for ec2 instance designed to deliver same app/content / shared storage.

--> EFS runs on NFSv4.2 protocol. NFS Port Number: 2049

Where we need to open NFS/EFS port on EFS Security Group.. 

1. Open NFS port on EFS SG inbound rule and add ec2 instance Security group as Source. ****
2. Open NFS port on EFS SG IB rule and add EC2 Instance Private IPs as source.
3. Open NFS port on EFS SG IB rule and add entire VPC IP CIDR as source..
4. Open NFS port on EFS SG IB rule and add anywhere/everywhere (0.0.0.0/0).. *Not recommended
5. Use Default Security group for EFS and Attach Default SG for EC2 instances also.. 


#!/bin/bash
sudo dnf install httpd -y
sudo systemctl start httpd
sudo systemctl enable httpd


Enhanced : Provides more flexibility and higher throughput levels for workloads with a range of performance requirements.
Enhanced with Elastic is best option..

Bursting : Provides throughput that scales with the amount of storage for workloads with basic performance requirements.


sudo mount -t nfs4 -o nfsvers=4.1,rsize=1048576,wsize=1048576,hard,timeo=600,retrans=2,noresvport fs-0246f2798ce101b13.efs.ap-south-1.amazonaws.com:/ /var/www/html/


===============


Cloudwatch : Its monitoring service, It monitors ec2 instances resource utilisation, provides metrics at aws console and we can configure alarms based on the thresoulds. 

CW has 2 types of monitoting.
1. Basic Monitoring.    : Graph updated for every 5 minutes
2. Detailed Monitoring	: Graph updated for every 1 minutes


With default CW metrics, we cannot monitor "memory" and "disk usage" metrics. We have to configure CW agent inside ec2 instance to get the addl metrics.

Metric : CPU Usage, network, Status check, disk usage
Threshould : "selected metric" usage exceeding 80% for 5 min or more..
Action : Do you want to stop/reboot/terminate/recover the instance.. ASG Trigger.. 
Notification : Send email via SNS (Simple Notification Service)


sudo dnf install stress -y

nproc				--> TO know cpu count

CPU Stress
stress --cpu 2 --timeout 600

memory stress
stress --vm 2 --vm-bytes 512M --timeout 600


==============

EventBridge : EB is a serverless event bus service that connects our AWS services, SaaS apps and custom apps using events. This allows us to build loosely coupled event driven architectures without writing complex polling logics. 

EB has 2 Primary use cases : 

1. Event Driven Process : React to event 
(ec2 instance stopped --> send notification)
(Someone opened 0.0.0.0/0 at SG --> Send notification)

2. Scheduled Process : Run task at a specific time / interval
(Satrat dev instance at 08 AM IST Everyday)

---

Event : A JSON message that describes a change in state

Event Bus : The channel through which events flow (default* / Custom / partner)

Rule : Defines which events to match and where to send them

Target : Destination of a matched event (SNS, Lambda, SQS ...)

Event Pattern : JSON filter that matches event based on fields

Input Transformer : Reshape the event before sending them to target.

Input path : Extract only what we need
Input Template : Use Input path info in simple language in input template

---
Usecase : EC2 instance state change alert
---

{
   "version":"0",
   "id":"b172d695-106e-84d3-800e-c2cf1e67cc32",
   "detail-type":"EC2 Instance State-change Notification",
   "source":"aws.ec2",
   "account":"655700896650",
   "time":"2026-04-23T02:12:44Z",
   "region":"ap-south-1",
   "resources":[
      "arn:aws:ec2:ap-south-1:655700896650:instance/i-0e3144f0091b31b97"
   ],
   "detail":{
      "instance-id":"i-0e3144f0091b31b97",
      "state":"stopped"
   }
}

---

input Path:

{
	"time": "$.time",
	"region": "$.region",
	"instance-id": "$.detail.instance-id",
	"state": "$.detail.state"
}

---

Input Template:

"At <time>, status of my ec2 instance <instance-id> changed to <state> in <region> region. Act Fast.!!"

---

User Case 2 : get alert when IAM user created or deleted

Pre-Req: Management trail must be enabled in "us-east-1 / N. Virginia" region.


{
  "source": ["aws.iam"],
  "detail-type": ["AWS API Call via CloudTrail"],
  "detail": {
    "eventSource": ["iam.amazonaws.com"],
    "eventName": ["CreateUser", "DeleteUser"]
  }
}

---

Task : Send email alert whenever anyone open 0.0.0.0/0 at SG level.


=======


Task : After launching an instance from GoldenAMI, ec2-user is not able to login into instance even after setting the password and changing the allowpasswordlogin to yes in sshd config file. 
Help to identify the issue.



===============

IAM Users will have 2 types of accesses.

1. Management console access : Username, Password and Sign-in URL.. Browser.. 

2. Programatic Access : AccessKeyID and SecretAccessKey.. CLI (cmd/powershell/shell)

Pre Req: aws.amazon.com/cli .. Download and install aws cli tools


=========

Step 1 : configure aws cli  
--> AWS AccesskeyID and SecretAccess will be stored as plain text
--> users home directory (folder .aws --> config & credentials).. 

aws configure

Provide AccessKey
Provide SecretAcesskey
Provide Region : ap-south-1
output format : none / json / text / table

sts : security token service

aws sts get-caller-identity


=======

Method 2 : AWS LOGIN (new and recommended option)

** Make sure to attach "SignInLocalDevelopmentAccess" policy to the IAM user. 

AWS login option is bit secure as token refresh for every 15 minutes. 
token stores in "~/.aws/login/cache/" location .json files.


--> aws login

--> aws login --remote				(No browser option --> copy url, enter in browser, get the code and copy and provide here)

=========

named Profiles for Multi Account

aws configure --profile dev			(accesskey & secretkey)
aws configure --profile uat 		(accesskey & secretkey)

aws s3 ls --profile dev				List s3 buckets from dev account

aws s3 ls --profile uat				List s3 buckets from uat account

aws configure list-profiles			--> list all configured profiles

========

TO know where the comamnd got strick / how command is executing:

aws s3 ls --debug

======

modify the output format based on our req.

aws iam list-users --output text
aws iam list-users --output table
aws iam list-users --output json

=====

aws s3 sync <local> <dest-path>


aws ec2 

ami-id
instance-type
keypair
securitygroup
count


aws ec2 run-instances --image-id ami-0e12ffc2dd465f6e4 --instance-type t3.micro --key-name awar08-kp --count 1 --security-group-ids sg-0f7cdd4cf77882524 --region ap-south-1 --tag-specifications 'ResourceType=instance,Tags=[{Key=Name,Value=my-cli-instance}]' 'ResourceType=volume,Tags=[{Key=Name,Value=my-cli-instance}]'


aws ec2 describe-instances

aws ec2 stop-instances --instance-ids i-0dc63c5aa813a24c4

====

To query/search particular info from long resource description, we can use "JMESPath Query" option. 

ex: aws ec2 describe-instances --query 'Reservations[*].Instances[*].InstanceId' --output text

=====

aws help

aws s3 help

aws s3 ls help

=====

https://kiro.dev/downloads/				--> based on your os, downlaod and install

=====

IAM ROles : allow ec2 instances / lambda functions / ecs / event bridge schedules / s3 buckets to call another service securely, without storing the iam credentials (accesskeyid and secret accesskey)


----

Systems Manager : 

preq : Create an iam role and atatch it to ec2 instance, make sure that role has attached with "ssm related polocies".

----

D: 27/04/2026

Vertical Scaling : Adding or removing resources (i.e; cpu / memory) to the same instance. 
--> We have to stop the ec2 instance to perform vertical scaling.

CRQ : Change Request : t3.micro --> t3.small

CAB / Change Advisory Board : We have to present our CRQ to CAB team/meeting.
Non-Prod : weekly 2 times
Prod : weekly 1 time

--> When it is planned to implement.? Ans : Saturday morning 05:00 AM - 09:00 AM
--> Is it client facing resource.? Ans : Yes
--> Do we have client approvals.? Ans : Yes
--> Does it involve any budgeting approvals.? Ans : Yes, Minimum cost change
--> Does this change required downtime.? Ans : Yes, Downtime required, Approx 15 min
--> Do you have an y rollback plan.? Ans: Yes, undo change
--> what are all the teams involved in this CRQ.? Ans: aws, production support, db, app/dev..
--> Have we created tasks for all the teams involved.? Ans: yes
--> Are you performing any Pre-implementation steps.? Ans : Taking snapshot/backup.. Stop services.. 
--> Who will perform sanity check / change validations.? Ans: App, Prod support.. 
--> Is this change already implemented in lower environments.? Ans : Dev --> QA --> UAT --> Prod

---

Horizantal scaling : Adding more number of instances to deliver our workload.. 


Load balancer Health Checks

Protocol: http
Port : 80
Path : /status.html
Interval : 30 Sec
Request timeout : 2 Sec
Healthy Threshoulds : 5 
Unhealthy thresholds : 5


ALB : Application Load Balancer : Layer 7 LB : Web Apps, Containers, microservices, APIs.. , Also, supports Path Based and Host Based routing.. We cant allocate a dedicated IP Address.
Supported Protocols : http, https

NLB : Network Load Balancer : Layer 4 LB : Low latency, high performance apps.. Supports Port/IP based routing..
Supported Protocols : TCP / UDP / TLS













